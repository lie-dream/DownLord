# 更新记录

按版本倒序整理已确认、影响使用或构建的变化。早期条目来自原 README 的 v0.4 / v0.3 / 往期摘要，不是正式 GitHub 发布日期证明；没有依据的日期不补填。本地安装包也不证明版本曾公开发布。

## 1.0.0 — 2026-09-22

### 新增

- 自有代码的 MIT 许可证与统一第三方许可 / 源码途径索引；关于页提供打开本地“第三方许可与声明”的入口。完整安装产物与断网打开仍待实际验收。
- 根安全政策，说明私密漏洞报告、脱敏材料与维护范围；私密渠道仍须在发布阶段启用并验证。
- 主列表顶部搜索框：按文件名或来源做字面子串匹配（忽略首尾空白，英文不区分大小写），只筛选当前已加载的任务列表，并与左导航 / 类别筛选叠加；×、空态“清除搜索”与 Esc 只清空查询、保留筛选，“全部开始 / 全部暂停”不受搜索或筛选影响。查询不保存、不联网，关闭应用后为空；不是历史页搜索，不做模糊或拼音匹配。

### 变更

- 全局限速语义改为每个“跟随全局”任务各自的上限，单任务限速可覆盖（0 = 本任务不限速，自定义值可以大于全局值）。多个任务同时下载时总速度可能超过全局值，不再是总带宽上限；改全局值对正在下载的直链任务即时生效，正在下载的视频任务在下次继续时生效；单任务限速重启后恢复跟随全局。设置页“下载限速(全局)”描述同步改写，不再写“均分”。
- README 改为产品、快速上手、功能限制、构建与合规入口；原版本历史迁到本文件，实机截图待补齐。
- 浏览器扩展支持说明沿既有实测记录统一为 Edge / Chrome 同属承诺范围；Firefox 扩展本版不做。区分扩展内 Cookie 与外部浏览器数据库读取，不泛化为全部 Chromium 已验证。
- 源码构建最低要求为 Node.js ≥ 22.12、npm ≥ 11；CI 继续固定 npm 11.11.0。
- 应用与扩展版本对齐 1.0.0，更新目标对齐公开仓库；仅配置对齐，不代表真实自更新闭环已通过。
- 保留未签名、开发者模式手动加载扩展及宽权限风险说明，不承诺签名或商店分发排期。
- 构建 / 测试依赖按 `npm audit fix` 更新（补丁 / 次版本，不进安装包）。

### 修复

- 设置页“下载限速(全局)”：单位从 KB/s 切到 MB/s 时，小数值不再显示为“0.0”并在失焦时被误提交为 0（如 10 KB/s 显示为 0.01）；按 Enter 提交后焦点离开输入框，有可见反馈（BT 组三个数值框同形态）。
- HTTP 403/404 与 aria2 码 22 的失败提示补上“若从浏览器发起，可回浏览器重新点一次取得新链接”的人工操作指引；不是自动重取、来源识别或保证重试成功。

## 0.4.0

### 新增

- DownLord Connector 浏览器扩展与本地通道配对；浏览器下载转交应用，独立确认窗口始终由用户确认，重复下载复用四选一，支持临时暂停接管。
- 网页媒体嗅探（默认关闭）：按标签页列出视频流 / 媒体文件、聚合分片计数，选中条目再转交应用。
- Cookie 来源增加“从扩展获取”：点击下载后由 DownLord 点名候选站点、扩展按需读取。暂借登录态留在应用会话内存，关闭或显式清除；交给单次 yt-dlp 的临时文件在进程退出时删除，并在启动时清扫异常残留。
- BT tracker 应用内按需自动更新和手动更新，失败如实提示并回退内置快照；入站可达自检只读本机接口，不联网、不改网络设置。

### 变更

- 扩展作为同版本 ZIP 与随安装包分发的可加载目录，需开发者模式手动加载，不上商店。
- 对当时交付的 tracker 更新和本机入站可达自检说明其边界：检测不等于自动打通端口，不保证上传速度或连接提速；当前范围见 README，不把后来的裁决倒写成当时决定。

## 0.3.0

### 新增

- BT / magnet / 本地种子下载，与直链和视频共用任务列表及历史。
- 先取种子信息、多文件选择、节点与上传状态；BT 内容独立放入默认下载目录的 `Torrents\`。
- 下载完成后的可选做种（默认关闭），分享率 / 时长上限、最大连接数、手动停止做种并保留文件与续传信息。

### 变更

- 单任务限速增加“跟随全局”。
- 历史重复检测按来源索引，提高大量历史下的查询效率。

### 修复

- 视频“最高 / 不超过某清晰度”输出名使用实际下载清晰度，与手选同清晰度一致，避免影响重复检测。
- 视频解析增加总超时；删除解析中的任务会中止解析进程，避免无限停留在解析状态。

## 0.2.0

### 新增

- Cookie 登录态下载、字幕下载、下载限速、分片加速。
- 重复检测、默认关闭的剪贴板监控、历史搜索与统计。
- yt-dlp 独立热更新和应用更新入口；入口具备不等于公开发布或真机更新闭环已验证。

## 0.1.0

### 新增

- 直链多线程下载与断点续传。
- 视频站点解析、清晰度选择、提取音频及批量添加。
- 按文件类别归类；直连、系统代理、手动代理三档。

---

最新使用与限制以 [README](README.md) 为准；第三方分发材料以根 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md) 为唯一维护源，安全报告见 [SECURITY](SECURITY.md)。
