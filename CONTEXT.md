# DownLord

DownLord 是一款 Windows 桌面下载器（Electron + React + SQLite），把 aria2 / yt-dlp / ffmpeg 三个成熟引擎包成统一体验：直链、视频、BT / 磁力走同一套任务模型、同一份历史。

本文件是**术语表**——统一「同一个概念该用哪个词」，供人与各协作模型共享同一套语言。
只收 DownLord 特有的概念；通用编程概念不收。**不写实现细节、不写方案、不当 spec** —— 那些属于 `docs/ARCHITECTURE.md` 与各功能的设计 / 计划文档。

> 相关文档见 [CLAUDE.md](./CLAUDE.md) 的「文档分层」。术语与源码冲突时**以源码为准**，并回头修正本文件。

## Language

### 任务与状态

**任务（Task）**:
一次下载的完整单元，从添加链接到落盘完成。三种 `kind`：`http` / `video` / `torrent`。
_Avoid_: 下载项、条目、job

**kind**:
任务的类型维度，取值 `http`（直链）/ `video`（yt-dlp 解析类）/ `torrent`（BT 与磁力）。
_Avoid_: `file`（历史误称，实际不存在此取值）、type

**状态（status）**:
任务在生命周期中的持久化位置，八态之一：`resolving` / `awaiting_selection` / `queued` / `downloading` / `paused` / `processing` / `completed` / `error`。会落库，重启后仍在。
_Avoid_: state、阶段

**解析中（resolving）**:
拿到链接但还不知道要下什么的阶段——视频在跑 yt-dlp 解析，磁力在向 DHT / tracker 求元数据。
_Avoid_: 加载中、pending

**待选（awaiting_selection）**:
已知有哪些可选项、等用户拍板的状态——视频等选清晰度，种子等选文件。
_Avoid_: 等待中、waiting

**处理中（processing）**:
字节已下完、yt-dlp 仍在合并音视频或提取音频的阶段。**不可暂停**。
_Avoid_: 后处理、转码

**扣留（parked）**:
查重命中后「既不入队也不落库、扣在内存里等用户决策」这一处境。**不是 status**，任务此刻不在状态机里。
_Avoid_: 挂起、pending、暂存

**做种（seeding）**:
torrent 下完后继续上传的处境。**不是 status**——持久化状态恒为 `completed`，做种只是运行时事实，重启即消失。
_Avoid_: 上传中、种子状态

**运行时态（runtime）**:
只活在内存、故意不落库的信息（限速覆盖、做种、上行速度、节点数、连接数、**暂借登录态**）。重启即回默认，这是设计而非缺陷。
⚠️ **暂借登录态是其中唯一敏感的一条**——它装的是用户的登录凭据，故它的「会话级」不只是取舍、更是安全承诺：连日志里的域名都不许留，否则**日志会比它记录的东西活得更久**（v0.4 Task 6 立）。
_Avoid_: 临时状态、缓存

**主列表搜索**（v1.0，2026-09-17）:
对主列表当前已加载任务的 `filename` / `source` 做字面子串展示筛选，与导航和类别按 AND 组合；不是独立历史页查询，也不改变全局批量操作或原始统计范围。2026-09-17 用户实机验收通过。三项未覆盖如实保留、由后续回归承接：「还没有下载任务」真空态未实测（无干净会话）、读屏未实测、「不搜目录 / 不新增 source 展示」仅有自动化。
_Avoid_: 全库历史搜索、网络搜索

### 引擎与执行

**引擎（engine）**:
真正干活的外部程序：aria2（直链 + BT）、yt-dlp（视频解析与下载）、ffmpeg（合并 / 转封装）。各自独立子进程，崩溃不拖垮主程序。
_Avoid_: 下载器、后端

**双执行器**:
「同一个任务接口、两套执行后端」的格局——aria2 常驻守护 + RPC，yt-dlp 每任务一进程。上层只面对统一的任务与进度。
_Avoid_: 多引擎、适配器

**gid**:
aria2 给每个下载分配的句柄。**只活在内存、绝不落库**，对业务层完全不可见。
_Avoid_: 任务 id、下载 id

**`.aria2` 文件**:
aria2 自己维护的字节级续传控制文件。断点续传归它管，DownLord 的库只管业务元信息。
_Avoid_: 进度文件、临时文件

**`.part` 文件**:
yt-dlp 的半成品文件。暂停即杀进程留下它，恢复靠同参数重起续传。
_Avoid_: 缓存文件、临时文件

### 保存位置

**落点（savePath）**:
任务最终写到磁盘的完整绝对路径，恒非空。
_Avoid_: 保存路径（易与下面的「类别落点」混淆）

**类别落点（类别的 savePath）**:
类别配置里的目录字段，**空 = 跟随默认目录**（随默认目录改变而实时改变），**非空 = 用户自定义的绝对路径覆盖**。与任务的「落点」同名但语义相反——那个恒非空，这个空才是常态。
_Avoid_: 分类路径、category dir

**跟随 / 自定义**:
描述某个类别当前用的是默认目录（跟随）还是用户指定的固定目录（自定义）。UI 上如实显示真实落点，不含糊。
_Avoid_: 默认 / 手动、自动 / 指定

**不回迁**:
改了目录只对**之后**的任务生效，已下载文件留在原地不搬。这是既定取舍，不是待修的遗漏。
_Avoid_: 不迁移、不同步

### 限速

**限速三态**:
单任务限速的三种含义：**未设 = 跟随全局（上限 = 当前全局值）** / **0 = 本任务不限速（覆盖全局）** / **N = 限到 N KB/s（可大于全局）**。全局限速只有两态（0 = 不限）。
_Avoid_: 限速开关、speed limit

**全局限速**:
每个「跟随全局」任务**各自**的上限，**不是总带宽上限**——N 个跟随任务同时下载时总速度可达 N × 全局值。两引擎语义一致：aria2 直链任务取全局默认值、运行中的跟随任务在全局改值时逐个追补即时生效；yt-dlp 视频任务运行中的进程不改速、下次继续时按新值。v1.0（2026-09-17 拍板、2026-09-18 用户实机验证通过）前 aria2 侧曾是总量硬上限，单任务 0 / N 无法覆盖（已修正）；「总带宽上限」作为独立设置属新增能力、已推后，v1.0 不提供。
_Avoid_: 总带宽上限、总量限速

### BT / 磁力

**元数据阶段**:
磁力链接下完种子信息、知道里面有哪些文件之前的那段时间。此时还不知道总大小，进度不可信。
_Avoid_: 预下载、metadata 阶段

**整包**:
不做文件选择、种子里所有文件全下。全选与整包等价。
_Avoid_: 全量、完整下载

**文件选择**:
多文件种子里勾选要下哪几个。下载开始后不改选。
_Avoid_: 筛选、过滤

**分享率（ratio）**:
已上传字节 ÷ 已下载字节。做种的停止条件之一。
_Avoid_: 上传比、share rate

**默认停**:
DownLord 的做种默认策略：下完即停止上传，做种是用户主动开启的可选项。定位是下载器，不是 P2P 客户端。
_Avoid_: 不做种、关闭做种

**入站可达**:
外界能否**主动连到**本机的 BT 监听端口。它决定做种上传能否发生，也影响下载时可用 peer 集的大小（swarm 里大量 peer 同样不可入站，双方互相够不着）。NAT / 校园网后默认不可达；有公网 IPv6 且路由器与系统防火墙放行才**可能**可达。DownLord **只检测与如实呈现，不承诺打通**。
_Avoid_: 端口开放、可连接、NAT 穿透

**tracker 表**:
给 aria2 的 announce 地址清单，分三态：**内置表**（随安装包分发的静态快照，**只在拉取失败时兜底**）/ **远端表**（从公开源拉到的最新表）/ **生效表**（aria2 当下实际在用的那份）。远端表拿到即**整表替换**，内置表不参与合并——否则快照里的死条目永不淘汰。
_Avoid_: tracker 列表、announce 列表

**追补**:
tracker 表更新后，对**已在运行**的 BT 任务逐个下发新表的动作。因为 `changeGlobalOption` **只对此后新增的任务生效**（2026-07-29 裸跑 aria2 实测），不追补的话当前任务会一直用旧表。属**尽力而为**——已验证调用被接受，未验证下一轮 announce 是否真用新表。
_Avoid_: 热更新（那指拉取动作）、同步、刷新

### 浏览器扩展（v0.4）

**扩展（extension）**:
装在用户自己浏览器里的那个附加组件。它是 DownLord 的**第二个可分发工件**，与主程序分开构建、分开安装。**它只上报意图，不做业务决策**——是否接管、落点、查重、分类都由主进程定。
_Avoid_: 插件、addon、浏览器端

**本地通道**:
扩展与 DownLord 之间那条只绑 `127.0.0.1` 的 HTTP 通路，靠**三闸分层**鉴权：token 走请求头 + **拒绝一切 CORS 预检**（网页 JS 由浏览器自己拦死，不靠我们判断）+ Origin 前缀校验（**纵深而非主闸**——本机程序能任意伪造 Origin，那部分靠 token 挡）。**只传意图，不传决策**；不做反向推送（MV3 的 service worker 会空闲销毁，长连接不可靠）。**默认关**——不装扩展的用户不该白开一个监听端口。
**协议分发表增删任何一行 → 必 bump `protocolVersion`**（v0.4 Task 5 立，**Task 5 收口核对：已按此 bump 到 `2`**；**v0.4 Task 6 拍板：新增 `video.intent` 行 + `cookie.offer` 由占位转正 → 再 bump 到 `3`**）：既然版本不等一律 409 拒绝、不尽力兼容，那「新增 type 却不 bump」本身就是在做尽力兼容——失配会表现为「别的功能都好使、新功能点了没反应」，是最难诊断的形态。
_Avoid_: IPC（那是主进程与渲染进程之间的）、RPC、桥、**双闸**（v0.4 Task 3 起为三闸）

**配对**:
用户把 DownLord 显示的配对码粘进扩展这一次性动作，此后长期有效。重新生成 token 会让旧配对失效。
**配对码只含 token**——token 是**身份**，端口是**连接参数**，两者正交：**改端口不需重新配对**，只需两侧地址各自对齐（扩展读不了文件系统、不会自动跟上，故 DownLord 侧改端口后必须明确提示「扩展里也要改」）。
⚠️ **「已配对 / 未配对」是 DownLord 侧的词**，判据是**握手**（`lastActiveAt` 有没有值）。**扩展侧不得共用这两个词** —— 它只知道「storage 里存没存过那串码」，无从判断码对不对，故 popup 那一行叫「**配对码**」、取值「**已保存 / 未保存**」（v0.4 Task 3 手测 B3/B4 踩出：共用同一个词会出现「连不上 DownLord」+「已配对」这种自相矛盾的组合）。推广口径：**两侧都有的词，若判定依据不同，就不许共用**。
_Avoid_: 连接、授权、绑定、**扩展侧写「已配对」**

**待活动**:
扩展连接三态的第三态（未配对 / 已连接 / 待活动），指「已配对，但本次启动尚未握手」。**刻意不叫「断开」**——握手是**事件驱动**的（sw 冷启动 / 打开 popup / 有真实业务请求），没有定期心跳，故「久无握手」既可能是扩展挂了、也可能只是用户一天没下载。分不清就不假装分得清，与「已连接」是同一种收敛。
_Avoid_: 断开、离线、未连接、超时

**接管（takeover）**:
用户在浏览器里点下载 → 扩展拦截并取消浏览器原生下载 → 交给 DownLord 下。**装了扩展即视为授权，默认开启**（与默认关的剪贴板监控相反——那是被动监听，这是用户主动点了下载）。
**「能接上」的判据是「受理」而非「已建任务」**：DownLord 表示收下并从此全权负责，扩展才取消原生下载；等到任务真建成再取消是做不到的（人类确认要多久不可知，而浏览器一直在写盘）。
**接管路径永不绕过用户确认**——这既是体验取舍，也是安全边界（详见「本地通道」的残余风险与 `docs/ARCHITECTURE.md` §7.6 的威胁模型重估，已于 v0.4 Task 4 重新论证完毕）。
_Avoid_: 劫持、拦截（拦截只是手段，接管是完整语义）

**接管判定**:
「这个下载要不要从浏览器手里抢过来」这一层判断。**与「类别 / 类别落点」那套扩展名清单是两回事**——后者管「抢过来之后放到哪个目录」、从不拒绝任何文件；接管判定管「抢不抢」。两者都会提到「文件类型」，但一个是目录归属、一个是接管与否，**放在同一个界面里必然被误解**（v0.4 Task 4 澄清中，人与 AI 各误解过一次）。
_Avoid_: 接管规则（易与类别的扩展名清单混为一谈）、过滤

**黑洞**:
「**没有任何一方对这次下载负责**」的状态——浏览器不下了，DownLord 也没接住。**必须先确认能接上、再取消**浏览器下载；接不上就别取消。这是接管功能的头号红线。
⚠️ 判据是**谁负责**，不是**谁在下**：用户在确认框里主动点「取消」**不算**黑洞（有人负责了，就是他自己），前提是确认框**如实告知浏览器那边的处置**。⚠️ 这句告知本身订正过一次：「浏览器那边**已经**取消」是一句**并不总为真**的断言——取消打在一个**已经下完**的下载上是静默无效的，故实装说的是「**已请求**浏览器取消……若它此前已下完，文件可能已在浏览器下载目录里」。**诚实的前提是那句话恒为真，不是听起来干脆。**反过来，任务在查重等环节**静默蒸发**（既不在库、也不在列表、无人应答）**算**黑洞，哪怕通道那一段完全正常。
_Avoid_: 丢失、失败

**临时暂停接管**:
让接管在一段时间内不发生，把下载交回浏览器自己处理。
**状态的唯一真源在 DownLord**，扩展 popup 上的开关只是**遥控器**——「是否接管」是决策，按 §7.2 必须留在主进程；且本地通道无反向推送，状态若存扩展侧，DownLord 只能显示「最近一次听说的」（与「待活动」同一种分不清）。暂停期间扩展照常上报意图，由 DownLord 回绝，**在可观察行为上等同于「完全不拦截」**。
_Avoid_: 禁用、关闭、**把 popup 开关当成状态所在地**

**嗅探（sniff）**:
扩展监听页面网络请求、识别出其中的媒体资源（m3u8 / mp4 等）。**popup 是唯一呈现处**——结果**不经通道上行**、不落库、关浏览器即清；分片流聚合成一条，不逐片罗列。
⚠️ **「popup 唯一呈现」意味着 DownLord 侧没有资源列表**（v0.4 Task 5 收口据实）：左导航那页叫「**浏览器扩展**」，只有连接状态 / 接管状态 / 嗅探引导，**没有嗅探到的资源、没有嗅探开关**——开关的真源在扩展侧，而通道无反向推送，DownLord 只能显示「最近一次听说的」。**别把它叫「嗅探页」**。
⚠️ **「聚合成一条」不是把分片拼起来**（自研分片拼接被 §6.1 堵死），而是**把分片归属到它们的清单那一条上**——分片本身不作为独立条目出现，归属判据是**同一标签页**，不猜 URL 路径。
⚠️ **呈现范围是「本页」不是「本会话」**：popup 只列**当前标签页**的资源，切了标签页就看不到原来那页的。「本会话」会让人以为切回去还在（v0.4 Task 5 拍板）。
⚠️ **「关浏览器即清」清的是存储，不是用户看到的数字**（v0.4 Task 5 Phase 1 真机实测得出）：`storage.session` 确实随浏览器进程退出而清空，但浏览器**恢复标签页后会自动重新加载页面、当场重新采集**——重开后 popup 不为零是**新采的**，不是残留。且前提是**进程真正退出**（Edge 的「启动加速」/「关闭后继续运行后台扩展」会让它关窗后仍常驻）。**唯一不依赖浏览器行为的立即清空动作是:关掉嗅探开关。**
⚠️ **未被用户选中的资源从不离开浏览器**——嗅探结果不经通道上行，DownLord 只在用户点了某一条时才收到那一条。这句话恒为真，可以直接写进 UI。
_Avoid_: 抓取、探测、捕获

**权限增长表**:
逐 Task 记录「向扩展 manifest 追加了哪条 permission、为什么」的审计线索（Task 2 `storage` → 3 `host_permissions` → 4 `downloads` → 5 `webRequest` → 6 `cookies`）。它是「最小权限」这条约束的**可核验形式**——权限只准按表增；要加表外的，须在该 Task 的 spec 里说明理由并**回写此表**。
⚠️ Task 3 的 `host_permissions` 只能写成 `http://127.0.0.1/*`——**match pattern 语法不支持端口**，故它实为**回环全端口授权**，不得表述为「只授权 52330」。Task 3 **不加 `alarms`**（改用事件驱动握手，见「待活动」）。Task 4 `downloads` 已落（2026-08-02），按表增、无表外新增。
⚠️ **Task 5 有表外新增**（v0.4 Task 5 收口回写，**已落 2026-08-11**）：`webRequest` **按表增、已落**；而 `host_permissions` 从 `http://127.0.0.1/*` 扩到 `<all_urls>`——**这一条是表外的**（实装 `["http://127.0.0.1/*", "<all_urls>"]`）。四条理由：
① **技术必需**：观察型 `webRequest` **要对目标 URL 持有 host 权限**，够不着就什么都嗅不到；而嗅探对象天然是「用户浏览的任意站点」，**没有更窄的写法能覆盖同一能力**。
② **为什么不用 `optional_host_permissions` + 动态授权**：该组合与**全局 `webRequest` 监听器**的配合**本项目未实测**，引入的是新的不确定性；选型原则是「走已实测的路径」。**v1.0 决议不上架商店,收窄权限属新增能力、已推后,留到真要商店分发时**。
③ **对冲措施**：嗅探开关**默认 OFF**（被动监听按「剪贴板监控默认关」那把尺子）+ 只处理 `tabId >= 0` + 日志零 URL + 结果只在 `storage.session`。
④ **诚实告知**：安装说明（根 `README.md` + `extension/README.md`）明写「**权限声明比实际行为宽**」——浏览器提示的是「读取您在所有网站上的数据」，我们实际只看请求地址与响应头。
⚠️ **不得当作「链条不变」放过**——权限增长表的整个价值就在于它是「最小权限」的**可核验形式**，悄悄扩张一次它就废了；表外新增必须像本条这样**显式记一笔**。
⚠️ **本表与协议 `type` 增长表口径独立、不互相引用**：本表管「向浏览器要什么权限」，那张管「扩展能对 DownLord 说什么」。**分开算**——混在一起会出现「因为协议没多要东西，所以权限也没增长」这类错误推理。
⚠️ **Task 6 `cookies` 按表增、无表外新增**（v0.4 Task 6 拍板）：扩展要读 `chrome.cookies` 才能在浏览器内部取到登录态——这正是第四档存在的理由（`--cookies-from-browser` 在 Chrome / Edge 运行中时读不到）。⚠️ **同时明确不加 content script 注入权限**：popup 那个「用 DownLord 下载此页视频」按钮**恒可点、不判断当前页是不是视频页**，就是为了不为一个我们本来就判断不了的启发式去要表外权限。
_Avoid_: 权限清单、permissions

**契约单向穿透**:
扩展与主仓之间唯一合法的类型通路：扩展**只能 `import type`** 主仓的 `src/shared/extensionProtocol.ts`，**禁值导入、禁碰 `ipc.ts`**（后者是主进程↔渲染进程的 IPC，与扩展无关）。`import type` 编译后消失，故耦合只停在类型层、**零运行时**——Electron 代码不会被 bundle 进扩展。由 eslint `no-restricted-imports` 钉死，不靠自觉。
_Avoid_: 共享类型、双向依赖


**清晰度选择**:
用户对画质的意愿，可以是具体高度、封顶（不超过某高度）、最高，或仅音频。
_Avoid_: 画质、分辨率选项

**清晰度标签**:
写进文件名的清晰度短标（如 `标题 [1080p].mp4`）。同一视频不同清晰度靠它区分，避免互相覆盖。
_Avoid_: 质量标记、后缀

**清晰度归一**:
把「最高 / 封顶」这类相对意愿，在解析后换算成实际会下到的那个具体高度，让文件名与手选结果一致、查重能命中。
_Avoid_: 标准化、解析清晰度

**Cookie 四态**:
视频登录态的来源，v0.4 起为**四态**：不使用 / 从浏览器读取 / 指定 cookie 文件 / **从扩展获取**。**只记来源与路径，绝不存 cookie 内容**。
⚠️ **前两档的区别不是「从哪拿」而是「谁去读」**：「从浏览器」是 **yt-dlp 自己**读浏览器的 Cookie 数据库（Chrome / Edge 运行中时通常读不到，这正是第四档存在的理由）；「从扩展获取」是**浏览器扩展按任务请求**把 cookie 提供给 DownLord。二者虽都「来自浏览器」，读取主体与通道完全不同。
⚠️ 第四档拿到的是**暂借登录态**，**只对扩展点过名的域生效**——用户不经扩展、直接把 URL 粘进 DownLord 且此前从未借过该站时，这一档拿不到 cookie。这是本地通道无反向推送的必然，**任何设计都堵不上**，故 UI 与错误文案必须如实交代。
_Avoid_: 登录方式、鉴权、**「Cookie 三态」**（v0.4 Task 6 起已是四态，旧称会漏掉第四档）

**暂借登录态**:
DownLord 从浏览器扩展收来、**只活在主进程内存**的那份站点登录凭据。名字编码了它的三条性质：**借**（不是自己的、要归还）、**暂**（会话级，关掉 DownLord 即消失）、**登录态**（不叫「授权」——那个词在本表已被「配对」与「接管」占用）。
**由 DownLord 点名、扩展给予**，从不由 DownLord 主动索取（本地通道无反向推送）；**未被点名的站点，cookie 从不离开浏览器**。同域再借即整份覆盖、不合并；消费时精确 host 快照（含空快照）优先，缺项仅允许同端口 `www.X ↔ X` 别名，仍不做一般父域回退（没有 Public Suffix List 就分不清 `a.github.io` 与 `b.github.io`）。点名协议不扩域，返回实际快照 host、最多两组，cookie.domain/value 原样保留（v1.0 Task 3）。
**不落盘、不落库、不写 settings、不写日志**——连域名都不写日志。喂给 yt-dlp 时会落一个临时 `cookies.txt`，那是它的**一次性投影、不是真源**，锚在**一次 yt-dlp 进程**、进程退出即删。
⚠️ **不加密落盘是刻意的**：持久化的登录态会**静默腐坏**（用户在浏览器里退登 / 换账号 / cookie 过期，DownLord 收不到任何通知），表现为「明明授权过却全报需要登录，而界面上那份授权看起来还好端端在」。会话级免疫这一整类问题。
⚠️ **它必然先于用户确认到达内存**——确认之后 DownLord 就无法再向扩展要任何东西了。这是通道形态的必然，**不是实现偷懒，别当 bug 修**。
_Avoid_: 临时登录态（「临时」已被 `.aria2` / `.part` / 运行时态三处 _Avoid_ 掉，且与那个真·临时文件双重占用）、授权、缓存的 cookie

### 查重

**查重**:
添加任务时检查「同源 URL + 同目录 + 同文件名主干」是否已下过或正在下，命中则扣留待用户决策。
_Avoid_: 去重、重复检测

**四决策**:
查重命中后用户的四个出口：**覆盖**（旧文件进系统回收站，非永久删）/ **跳过** / **重命名**（加序号并存）/ **打开**（直接打开已有文件）。
_Avoid_: 冲突选项、resolve

**同名主干（stem）**:
文件名去掉最后一个扩展名的部分。查重比它、不比扩展名——因为下载前预测的扩展名未必等于实际扩展名。
_Avoid_: 文件名、basename

### 网络与合规

**代理三档**:
`system`（跟随系统代理）/ `manual`（手动指定）/ `direct`（直连）。
_Avoid_: 代理模式、proxy 设置

**「已连接」**:
代理状态里指「能连到代理端口」，**不承诺墙外可达**。这个措辞是刻意收敛的。
_Avoid_: 可用、正常

**诚实（honest）**:
本项目的一条价值观，也是验收口径：能力边界如实说、失败原因如实报、做不到的不承诺、推迟的显式记录在案、真机没测的标注「归用户手测」而不谎报通过。
_Avoid_: 保守、克制

## 工程流程

> 这几个词在本项目有确切分工，跨模型协作时最易混淆。

**Task**:
一个版本内的功能单元，如 v0.3 有 Task 1–5。每个 Task 先出设计 + 计划再实现。**跨版本会撞号**（v0.1 / v0.2 / v0.3 各有 Task 1），引用时务必带版本前缀。
_Avoid_: 任务（与下载任务撞词——指开发单元时一律用英文 Task）

**Step**:
一次可独立执行的派发单位，自带可单独复制的完整提示词；其它 Step / 会话不会自动在场，后续执行所需的新结论须内联到接收 Step，事实记录留在既有设计 / 计划文档。**Step 1 恒为「只设计不写码」**。
_Avoid_: 步骤、阶段

**Phase**:
plan 里的实现阶段，含目标 / 影响文件 / 完成标准 / 验证 / 提交检查点。
_Avoid_: 阶段（与 Step 混用会失准）

**Step ↔ Phase 映射**:
**默认偏移 1**：Step 1 只设计、不对应任何 Phase；此后 Step N = Phase N−1。
⚠️ **不是铁律**——某个 Phase 若「两半的产出可各自验证、互不阻塞」就会被**拆成两个 Step**（已多次发生，v0.4 Task 1 / Task 4 与 v0.3 Task 5 均如此）。此时偏移量作废，**以该 Task 提示词显式给出的映射表为准**；Phase 的条数不因 Step 拆细而改。**用户实机执行的手测卡口不编 Step 号**，也不参与映射。
_Avoid_: 一一对应

**门禁**:
合入前必须跑绿的自动化检查：`npm test` + `npm run typecheck`（**node + web + extension 三个 project**，v0.4 Task 2 起）+ **`npm run lint`** + `npm run build`；收口时另加 `npm run package`。**四件套的名字与条数不变，但覆盖面自 v0.4 Task 2 起含浏览器扩展**——`npm test` 同时扫 `src/` / `extension/` / `scripts/` **三个测试根**（`scripts/` 是构建脚本纯函数单测的落点，不列此根它就永远不跑），`npm run build` 在 `electron-vite build` 之后连带构建扩展，`eslint .` 天然扫到 `extension/`。lint = `eslint . --quiet`（只报 error），**判据是 0 error**——2026-07-28 已把跨版本积累的 30 个 error 全部清零，此后**新增 error 必须当次修掉，不得再以「既有基线」放行**。要连 warning 一起看用 `npm run lint:all`:2026-09-22 实跑跟踪面 458 warning = `prettier/prettier` 454(71 文件;格式化那半刻意不做:一次性全量格式化会产生跨库巨大 diff、与收口签收可读性冲突,且 `--quiet` 的 0 error 判据不受影响)+ `react-hooks/exhaustive-deps` 3(`TasksContext.tsx` ×2、`ThemeContext.tsx` ×1,属**刻意的 mount-only / latest-ref 模式**,按提示补依赖反而会让订阅反复重建,故如实保留、不硬改)+ unused eslint-disable directive 1(`SettingsPage.tsx`,指向同一规则);本地未跟踪的探针目录另有噪音,不计入。换行已由 `.gitattributes`（`* text=auto eol=lf`）钉死为 LF，此前两万余条 CRLF/prettier 噪音已根除。
_Avoid_: CI、测试

**三层回归**(v1.0 Task 6 立):
收口时三棵树各自签、互不当证据:① **内部树**(`npm run gate` + `npm run package`,四数只从 `npm test -- --spec` 取,基线四数在固定起点实读、不沿用历史数字)② **候选发布树**(`release:prepare` → `release:check` 推送准入阶段)③ **发布树 CI**(推送后真 CI 绿)。本地 gate 与发布树之间的差额(剥离造成的缺项)须显式列表。反向探针不全量重跑历史,只重跑被点名有缺陷的判据,每条附正反对照。
_Avoid_: 全量回归(不分层就分不出哪层没签)、CI 通过即收口

**§7 红线**:
`docs/ARCHITECTURE.md` 第 7 章「禁止破坏的逻辑」七条。任何版本任何 Task **不得破坏或重新设计**，除非用户明确决策。
⚠️ **签收基准 sha 必须写死，禁用 `git merge-base HEAD main`** —— 本项目一直在 `main` 上直接开发，该命令在 main 上是**恒真的假绿灯**（v0.4 Task 4 收口踩过一次）。**版本级签收的基准 = 上一个版本收口的那个 commit**（本项目不打 tag，收口 commit message 即纯版本号，如 `v0.3`）。
⚠️ **Task 级与版本级口径刻意不同，不是笔误**（v0.4 Task 7 立）:**Task 级**未触及的红线一律标「**—**」，不得写成「已签收」（v0.3 收口教训）;**版本级七条全部实签，一条都不许标「—」** —— 版本级的问句是「整个本版有没有破坏 §7.x」，只要**有任一 Task 触及过**就必须签收。直接把 Task 级口径搬到版本级，会让「只被一个 Task 触及」的那几条**变成没人签**。
_Avoid_: 硬约束、规则

**版本三方一致**:
发版时必须相等的三个版本号:① `package.json.version` ② `extension/manifest.json.version` ③ **双产物文件名**（`downlord-<版本>-setup.exe` / `downlord-extension-<版本>.zip`）。构建期自动断言。
**单一真源是 `package.json`**，manifest 的 version **由构建脚本注入**（源 manifest **不含 `version` 字段**;`scripts/build-extension.mjs` 步骤 **0b** 与 `extension/build.test.ts` 的 **A7** 主动禁止它出现 —— **加占位会当场 `exit(1)` + 测试红**,这是刻意堵死手抄回潮）;断言只是兜底，不是主保障。与「buildId 两入口注入 + 比对」同族:**两处手写靠断言卡，是「单向指针必然遗忘」的同构形态**。
⚠️ **关于页显示的版本不是第四方** —— 它读自 `package.json`，断言它等于自证;「关于页显示对不对」归**签收前置的装机核对**，**不重复计一件事**。
⚠️ **不做扩展独立编号** —— 两个工件的兼容矩阵已由 `protocolVersion` 承担，再来一套是重复计数。
_Avoid_: 版本一致性、version sync

**收口**:
一个 Task 或一个版本的最后一步：端到端回归 + §7 逐项签收（带 git diff 佐证）+ 文档据实回填 + 门禁全绿 + review 落盘。
**收口 commit 的 message 用 Task 级名或纯版本号**（例如 `v1.0Task7` / `v0.4`），不带 Step 后缀——收口 Step 即完成该 Task，这是用户的命名惯例（2026-09-17 确认）。
_Avoid_: 结束、验收、收尾

**冻结 commit / 收口 commit**(v1.0 Task 6 立):
版本收口的两阶段内部提交。**冻结 commit**(Step 级名)= 全部代码 / 文档改动 + 草稿 review + 正式终闸通过后的那次提交,是候选发布树的来源;**收口 commit**(纯版本号,如 `v1.0`)= 发布完成后只回填内部文档(review §0 / §10、里程碑、TODO、术语表)的那次。两者之间只有推送 / CI / Release 等对外动作;推送后的回填**不得触及任何进发布树的文件**,碰了就是下一次发版。review §0 记冻结 commit 与公开提交,不记收口 commit 自身(自指,由下一版本开工时读)。
_Avoid_: 发布 commit、最终 commit(分不出两阶段)

**提交检查点**:
计划 / 提示词里出现的「commit 节点」。**是给用户的**：AI 到此停下、汇总改动交审查，绝不自行 `git commit` / `push`。
_Avoid_: 提交点、commit 步骤

**归用户手测**:
自动化覆盖不到的实装路径（真引擎、真种子、真发布、装卸载）由用户实机验证。AI 如实标注、不代为判定通过。
_Avoid_: 待测试、TODO

**签收前置**:
收口时**必须拿到实测结果才能签收**的手测项,与「归用户手测」相对——后者如实标注、不阻塞交付。哪些项属于前置,由该版本收口前的澄清逐项拍板,**不默认全归用户**。
⚠️ **「已有实测结论」的项仍可列为前置，但以引用既有证据结清、不重跑**（v0.4 Task 7 立;v0.4 前置④ tracker 拉取即此形态——2026-07-29 已实测通过，收口只做记账）。**前置≠必须当场跑**，它约束的是「有没有实测证据」，不是「什么时候跑的」。
⚠️ **有真实失败面的新链路，不因「内容更贵」而整条豁免**:该降级的是**门槛**不是**覆盖**（v0.4 把「真会员视频取 cookie」降级为「任一需登录站点」列入前置——验的是同一条链路，只是内容更贵;真会员那档仍归用户、不阻塞）。
⚠️ **引用既有证据的前提是同源**(v1.0 Task 6 立):实机跑的那个构建与收口源码必须同源,判据是 **hash 守卫**;实机之后源码又改过(哪怕只是追补)而没有第二次实机记录的,不得引用,须在最终包重跑。v1.0 Task 7 M01–M09 引用、Task 8 M01–M07 重跑,即按此分的。
_Avoid_: 必测项、阻塞项、blocker

**hash 守卫**(v1.0 Task 6 立):
引用既有实机证据时的机器前提:收口时重算该 Task 受测源码文件的 SHA256,与其收口 review 登记值逐一相同才准引用;任一不同,该 Task 的实机子集在最终包重跑。引用写法固定「证据 = <review 路径> §x.y · 实机日期 · hash 守卫结果」,不写自己的 PASS。它守的是「证据对应的字节没变」,不守「行为没变」——源码同字节但依赖 / 引擎版本变了仍可能失效,那一层靠三层回归。
_Avoid_: 源码校验、一致性检查

**有界遗留**:
已知没做完、但**影响范围说得清且不扩散**的缺口:写明成因、影响边界与后续落点,当场记录在案。是「诚实」在验收环节的具体形态,**不是**「先记着以后再说」的托词。
_Avoid_: 已知问题、技术债、待办

### 开源发布(v1.0 起)

**内部版 / 公开版**:
同一份文档的两个形态。**内部版是唯一真源**,住在仓库原位(`CLAUDE.md` / `docs/PRD.md` / `docs/ARCHITECTURE.md` / `docs/DESIGN.md` / `CONTEXT.md`),含指向 `docs/v*/` 与内部工作流的引用;**公开版**住在 `publish/`,是发版时覆盖进发布树的那一份。**单向派生,不并行演进**——公开版只在发版时从内部版重新导出,平时不动。当前公开版对象为上述五份(包含术语表、不含 `AGENTS.md`);历史手工复制且后续未维护的 `AGENTS.md` 不再作为交付对象,不恢复、不生成公开版、不登记同步基准。理由见下方「漂移检测」。
_Avoid_: 精简版、对外版

**公开版改写**:
公开版从内部版定向改写,保留对外有效的完整结论、必要理由与能力/安全/合规边界,不做摘要或机械删链接。A 类导航移除或改成公开有效入口,B 类溯源去引用而不去实质约束,C 类内部工作流删除仅服务内部管理的部分、将仍适用于公开开发的约束改写为只依赖公开树即可执行;内部计划、待办及历史讨论不得转成对外承诺。

**公开术语表**:
由内部 `CONTEXT.md` 单向派生并登记同步基准的公开术语版本,保留公开用户与开发者需要的定义,不保留纯内部任务管理条目。公开解释不得依赖被剥离资料;内部术语表照常保留完整内容,不因公开改写删除内部术语。

**发布树**:
推向 GitHub 的那棵文件树 = 本地 `main` 的树 − 剥离清单 + `publish/` 覆盖。由脚本构造,按下述「发布历史」规则形成公开提交;**本地 `main` 的完整 commit 历史永不推送**。首次选 orphan 起点而非 `git filter-repo` 重写历史,是为了从一开始就不连接内部提交链;推送前的发布树可重建重来。
⚠️ **CI 跑在发布树上,与本地门禁验的不是同一棵树**(v1.0 Task 1 记):`.github/workflows/ci.yml` 触发于 push,而推上去的是剥离后的那棵 —— **「CI 跑绿」与「本地 `npm run gate` 全绿」是两个不同的结论**,差额须显式算过,不能互相当证据。连带一个更基本的问题:**`ci.yml` 自己在不在剥离清单里** —— 若在,它永远不会跑。
_Avoid_: 公开分支、发布分支

**发布历史**:
发布树在 GitHub 上形成的提交链:首次以 orphan 起点建立,后续每次发版只以前一次公开提交为唯一父提交追加快照,不连接内部 `main` 的提交历史。正常发版不重建 orphan、不强推覆盖历史,已有版本标签保持不动;公开历史记录发版差异,不记录内部逐步开发过程。

**发布准备 / 发布检查**:
发布准备(`npm run release:prepare`)在独立目录构造候选发布树,不切换当前工作分支;发布检查(`npm run release:check`)复验来源与同步基准并执行发布断言,按“推送准入 / 正式发版准入”区分阶段报告、当前阶段检查失败返回非零,不自动修文件。二者共用 `scripts/release.mjs` 这一 Node.js ESM 入口,职责止于构树与检查,不包含打包或远端发布。

**剥离清单**:
不进发布树的东西。**注意它与 `.gitignore` 是两回事**——剥离清单里的东西**照常在本地入库、有完整 git 记录**,只是不进发布树。
_Avoid_: 忽略清单、排除项

**同步基准登记**:
`publish/sync-base.json` 是内部源与已审阅公开版的逐文件对应登记,含 `schemaVersion` 及每份文档的三个仓库相对路径 `sourcePath` / `publicPath` / `targetPath`、`baseCommit`(完整内部 commit ID)和 `publicSha256`(已审阅公开版内容哈希),自身不进入发布树。它仅覆盖需要改写后公开的文档,不登记整份剥离的内部资料或直接保留项,不要求两棵树或内外两版内容相同,也不能代替对改写正确性的审阅。

**漂移检测**:
发布前的机器判据:逐文件比较当前内部源内容与同步基准 commit 中的内容,并核对公开版内容与已登记哈希;任一不符即**卡住发布并列出差异**,无关仓库提交不构成文档漂移。只在审阅后更新同步登记(内部改动不影响公开版时可确认「无需改写」),`release:prepare` / `release:check` 不得自动刷新基准或哈希消除报错。
_Avoid_: 同步检查、一致性校验

**许可材料溯源**:
按实际随包二进制对应的版本/构建取得许可证原文、版权及授权声明和发行材料要求保留的第三方许可,优先原发行包,其次为可追溯的对应版本/构建上游文件。记录来源、对应版本/构建和取得文件的哈希;无法确认对应关系或许可记录冲突时停下补事实,不得以通用文本或上游最新版冒充当前材料,取得材料也不等于合规核验通过。

**第三方声明 / 许可证全文**:
根 `THIRD-PARTY-NOTICES.md` 是版本、许可证与上游发行页等汇总信息的唯一维护源，打包时自动复制为 `<resources>/THIRD-PARTY-NOTICES.md`，由关于页提供离线入口，不另建内嵌 Markdown 阅读器或人工维护第二份。声明不替代许可证全文，三引擎许可原文仍随包放在 `<resources>/bin/`。

**发布断言 / 待核验**:
发布断言以实际候选发布树为对象，按显式规则逐项检查剥离残留、内部版特征串、链接与敏感信息，并与同步基准等检查共同判定各阶段准入，时序见“推送准入 / 正式发版准入”。通过必须有可定位的已执行检查结果，漏检、执行失败或尚待核验均不是通过；仅明确列出、依赖本次推送才生效的自有仓库网页链接可在推送阶段暂待核验，正式发版前必须补验通过，报告中的敏感值须脱敏。

**公开截图**:
公开截图是为公开 README 呈现实际交付界面的实机图，核心清单覆盖主界面、视频下载与扩展使用，必要时补一张扩展加载指引，统一存放于 `docs/assets/screenshots/` 并保留在发布树中。沿用“AI 列要求 → 用户截图 → AI 嵌入排版”的分工，截图须单独人工检查敏感信息，不以原型或生成图片冒充实现。

**公开更新日志**:
根 `CHANGELOG.md` 按版本倒序记录对使用、升级、安全或源码构建有影响的重要变化，而非内部 commit / Task 流水账或未来排期，并承接 README 已有、可确认的版本历史，README 不再重复维护整段历史。尚未发布的版本标“待发布”，正式日期到发布时再填，不预填或推算日期，也不从构建产物的存在性推定发布历史。

**安全报告渠道 / 私密报告**:
根 `SECURITY.md` 约定以 GitHub Private vulnerability reporting 为主渠道，入口须在发布前启用并确认可用，并提醒不要在公开 Issue 中披露尚未修复的漏洞细节。私密报告不等同于对匿名或免登录提交的承诺，未经核验的账号或登录条件也不写成确定限制。

**安全维护范围**:
安全维护面向最新稳定版，不承诺向旧版本分支回补，但不拒收旧版的疑似漏洞报告。安全报告遵循最小必要、材料脱敏与协调披露，维护者尽力核实沟通，不承诺固定响应或修复期限，也不将扫描通过视为无漏洞证明。

**更新目标 / 更新启用策略**:
开发态 `dev-app-update.yml` 与正式打包发布配置的公开仓库目标统一为 `lie-dream/DownLord`，owner / repo 可以入库，令牌、密码等凭据不得入库。统一更新目标不改变开发态更新检查的启用策略或触发时机，也不构成自更新链路已验证闭环的证明。

**发布卫生**:
发布卫生以本次候选发布树与交付产物的安全、正确性为边界，既定敏感项清理、登记同步与忽略规则必须落实，不能仅凭 Git 忽略状态推定包内安全。它不等于清空本地旧产物、改写内部历史或顺便做 Git 对象整理与全库格式化；内部资料和历史保留，本次交付文件须与历史产物明确区分。

**版本口径清理**:
版本口径清理是按既有裁决修正现行文档中的失效版本承诺，先维护内部源再同步公开版，不等于把旧版本号批量替换为新版本。真正的历史叙述保留历史语义，旧设计 / 计划 / 评审记录不追改；验收看现行口径是否仍有失效承诺，而非全库旧版本字符串零命中。

**项目公开署名**:
项目公开署名指应用元数据的 `author` 与项目 MIT 许可证中的项目版权署名，统一使用 `lie-dream`，不附私人邮箱。版权年份须有事实依据，第三方许可证中的原始作者与版权声明保留原样，不随项目署名替换。

**独立发布仓库**:
用于保存公开发布树及其独立历史的本地 Git 仓库，作为独立的公开发布工作区，不与内部研发仓库共用 `.git`，也不是内部仓库的 Git worktree。它只接收派生后的公开内容、以本地 `main` 对应公开远端 `main`；不复制内部历史及过程资料，不整包复制依赖目录、缓存和旧产物，额外空间主要为发布树与公开历史，若用于构建则依赖和产物另算。

**冻结发布树 / 正式发布产物**:
冻结发布树是锁定本次对外内容后、用于构建正式安装包和扩展 ZIP 的同一份公开树，正式产物须记录对应的发布树标识及自身哈希，不以内部开发目录里的旧包替代。依赖与三引擎作为明确的构建输入，剥离造成缺项时先处理并记录差额、不退回内部树出包；内部树本地门禁与发布树 CI 仍分别签收，不由产物同源要求替代。

**公开 Git 身份 / 元数据隐私**:
维护者的公开发布提交名称使用 `lie-dream`，邮箱优先采用该账号实际提供、经用户确认的 GitHub noreply 地址，不使用私人邮箱或猜测值，且只设置发布仓库级配置。推送前须核对待推送提交的 author / committer 与附注标签（如有）的 tagger，并检查提交及标签说明不夹带私人信息或内部路径，文件内容检查不能替代 Git 元数据检查。

**推送准入 / 正式发版准入**:
推送准入指允许将候选源码公开前必须完成的检查：内部树本地门禁及发布树同步、剥离、敏感信息、本地链接、Git 元数据等均须通过，只允许逐项列明、必须依赖本次推送才生效的自有仓库网页链接保留“待上线核验”。正式发版准入还须取得对应公开提交的 CI 通过证据并补验这些链接，有失败或待核验项则不得发布正式 Release / 安装包；源码推送已对外可见，安全与隐私检查不得后置，其他既定验收要求不因分阶段而取消。

**发布 runbook**(v1.0 Task 6 立):
维护者本地的发布操作流程文档(不随源码公开),由维护者亲自执行,**内部文档、不做公开版、不登记同步基准**。形态 = 五阶段(冻结 → 推送准入 → 推送与 CI → 正式发版准入与出包 → Release 挂载与发布后核对)× 每步五字段(执行方 / 命令或动作 / 预期可观察结果 / 失败处置归回滚哪一档 / 证据落点);首发一次性步骤(orphan 起点 / 独立发布仓库初始化与公开 Git 身份 / 私密报告入口启用 / 更新目标确认)单独成节。机器判据只复用 `release:prepare` / `release:check` / `package`,不新增发布脚本;推送 / Release 挂载 / 仓库设置恒为用户人工步。它在敏感信息扫描面内,公开 Git 身份、本机路径、用户名一律用角色名或占位写。
_Avoid_: 发布脚本、发布清单(不含执行方与失败处置就不是 runbook)

**探路跑 / 正式终闸**(v1.0 Task 6 立):
敏感信息终闸的两种跑法。**终闸三道** = 内部树 `npm run scan:secrets`(扫全部跟踪文件,含本 Task 新写的 review / runbook / prompt / TODO)+ 候选树 `release:check` 推送准入阶段 + 独立发布仓库的 Git 元数据核对,缺一不推送。**探路跑**不限次、不签人工项(PENDING 照留),反向探针(注入假凭据形态字面 → 报告出现新命中 → 移除归零)只在探路期做、注入物不进任何 commit;**正式终闸**只在全部跟踪文件改完 + `git add` 后跑一次(来源集合只取索引条目),用户对该候选重签人工项一次。推送前此后任何跟踪文件改动(含 review 补一个字)= 换候选 ID 整套重跑 + 重签;推送后的回填只重跑 `scan:secrets`。记录只写工具名 / 候选 ID / 命中数 / 处置 / 报告落点(`out/` 下不入库),不抄命中原文、不写邮箱 / 本机路径 / 载体 / 子证明 hash。
_Avoid_: 最后一次扫描、终检

**发布后核对**(v1.0 Task 6 立):
Release 挂载后由用户人工做的五项:① 从 Release 页面下载双产物,SHA256 与 review 登记一致(不用本地文件代替)② 用下载的安装包重装、关于页版本正确、点「检查更新」一次预期「已是最新」且无报错——这只证明更新检查能到达公开仓库的发布资产,**不证明升级闭环**(升级闭环留到首个 v1.0.x 验)③ 公开仓库页面:README 渲染、SECURITY.md 私密报告入口可见、LICENSE 与第三方声明在树中 ④ 依赖 Release 存在才生效的链接在此补验,与推送即生效的链接分开登记 ⑤ CI 在 Release 对应 commit 绿的链接记入 review。v1.0 直接发正式 Release、不先发 pre-release(更新检查默认忽略 pre-release,先发预发布版会让 ② 失真)。
_Avoid_: 上线检查、冒烟

**回滚三档**(v1.0 Task 6 立):
发布失败的处置按「对外可见了多少」分三档。**档 A 推送前**:什么都没公开,不叫回滚,内部树修 → 换候选 ID 重冻结(探路跑本身就是这档的演练)。**档 B 推送后、Release 前**:源码已公开,不强推、不重建 orphan,内部修 → 新候选 → 以前一次公开提交为父追加快照;唯一例外是推出去的树里发现敏感信息——先废掉该凭据(删提交不等于没泄露),再删远端分支重建 orphan 重推(首发特例,因尚无公开历史可保)。**档 C Release 后**:不在同一版本号下替换任何产物,产物坏或严重缺陷 → Release 改标 pre-release 并撤下附件,修复走 v1.0.1;已发布的版本号永不重用。🔴 **触发判据只有两类**:敏感信息泄露 / 产物不可用(装不上 / hash 不符 / 三方版本不一致),其余缺陷一律不回滚。档 B / C 不演练,review 如实标「未演练」。
_Avoid_: 撤回、回退(不分档就分不出哪些真的回不去)

**产物面 / 构建期面**:
一个依赖或组件**是否随安装包分发到最终用户手里**的分界,用来给漏洞、许可证义务等定影响面。**产物面** = 以 node_modules 形态进 asar 的 + 被 vite bundle 进 renderer 的 + 被 esbuild bundle 进 extension 的;**构建期面** = 只在构建 / 测试 / lint 与 CI 里跑的。
⚠️ **它与 `package.json` 的 `dependencies` / `devDependencies` 分类不重合,不许拿后者代替前者**(v1.0 Task 1 立):`react` / `react-dom` / `@fluentui/react-components` 声明在 **devDependencies**,却经 bundle 进了产物、分发给每个用户。故 `npm audit --omit=dev` 的数字**不是**用户面漏洞数 —— 两个方向都不可靠,必须实证分档(v1.0 Task 2 实测:全量 9 / `--omit=dev` 3 / A 档 1,这次是偏高)。
⚠️ 分档结论**可翻案**:构建期面的东西若后来被发现进了发布链路,当场升为产物面。正因如此,**每次分档必须写出依据**(如「出现在 renderer bundle 产物中」),否则翻案时只能把功课重做一遍,而不是核对一遍。
_Avoid_: prod 依赖 / dev 依赖(那是 `package.json` 的分类,与本分界不重合)、生产 / 开发依赖

**规则级假阳 / 实例级假阳**:
扫描类判据(敏感信息、许可证、措辞红线)误报的两种形态,**处置相反**;**判据是「同类的东西以后还会不会源源不断地再出现」**。
**规则级** —— 规则本身写错或过宽,同类命中会无限再生(如媒体扩展名 `ts` 撞 TypeScript,一条 grep 捞出 280 个源码文件)→ **收窄规则并重跑,不在命中报告留条目**;但须在脚本注释与报告里各记一笔「本条因 X 收窄过」,否则后人既不知道它为何这么窄、也可能顺手放宽回去。
**实例级** —— 规则正确,只是这一条命中确实不是秘密(如测试夹具里的示例 token)→ **留在命中报告里标「假阳 + 理由」,不动规则**。它有限、可穷举。
⚠️ **与安全清单里的「路径不可达」是两回事,纪律不可互换**:那个是「条目确实不符、只是风险到不了」,**必须留在清单里**;这个是「压根就不是那回事」。混用会让 280 条源码躺进清单、把真命中埋掉(v1.0 Task 1 立)。
⚠️ 收窄规则**必须跑双向对照**(真命中仍命中 + 假阳归零),只验一边就是恒绿的假绿灯。
_Avoid_: 误报(不分类就定不出处置)、噪音

**文案审视**:
写手测清单时,对每一条用户可见文案额外做的一遍**正确性**检查。与清单本身要验的「实装是否符合文案」相互独立,**由 AI 在写清单时逐条过**,不等用户手测。
成因(v1.0 Task 2 立):清单的「预期」栏必须**逐字抄现状**(否则用户照字面在界面上找不到,那条验收就白跑),但这样一来**凡是文案本身就错的地方都会被清单判为通过** —— 整份清单对「文案正确性」这一整类问题**系统性失明**。现成的例子是关于页那句「代码签名计划在 v0.3」,而代码签名已四度推后。
⇒ 清单每条开两栏:「预期(抄现状)」归用户验,「文案审视」(是否诚实 / 是否含已被推翻的承诺 / 是否让人困惑)归 AI 审,审出问题当场记为缺陷。
_Avoid_: 文案检查、走查、UI 评审

**如实说清单**(v1.0 Task 3 立):
「**如实说**」这个交付物的**可核验形式**:每条四字段 —— **逐字文案 / 展现位置 / 钉住它的用例名**,外加条目号。
🔴 **逐字文案的真源在这张清单里**,不在实现代码里,也**不再往 `docs/DESIGN.md` 抄一份**(双真源必然分叉)。
🔴 **无处可钉的必须显式标「无自动化守护 · 靠 review」,不装作有** —— 装作有比承认没有更危险。
**成因**:「如实说」若只落进代码,就没有任何东西钉着它不被下一轮改掉。现成的反面教材是「不回迁」与「其他类目录恒等于默认目录」这两句 —— 它们**只活在文件头注释里,UI 上查无此物**,而这正是它们被当成缺陷重新捡回来的原因。
_Avoid_: 文案表、话术清单、UI 文案库

**不变量类用例**(v1.0 Task 3 立):
断言对象是**不变量**而非某个具体取值的测试:判定顺序、零回归、「逐字节相同」、否定断言(「不出现任何 X 式表述」)。
🔴 **未经用户明确授权,它变红只准改实现,不准改用例**;判据纠偏须先明确授权边界并记录正反对照,不能自行当作放宽护栏的许可。与之相对的是「断言对象就是本次要改的那个事实」那一类,后者可以改,但每处都要进**用例改动表**记 `旧值 → 新值 → 条目号`。
🔴 机器兜底:`git diff --diff-filter=M -- '*.test.*'` 的每个 hunk 都要在用例改动表里有对应行。⚠️ **`--diff-filter=M` 不可省** —— 不带它会把本轮**新增**的测试也算进来、误判成「改了既有测试」。
**成因**:改行为必然要改一部分测试,而「改实现顺手改用例」与「正确地更新用例」**在门禁上输出一模一样**;不给出可判定的分界,护栏就会在全绿中静默消失。
_Avoid_: 回归测试、核心测试(不加限定就分不出「能不能改」)

**排除项 / 豁免**(覆盖率,v1.0 Task 1 起):
两个**方向相反**的词,不许混用。**排除项** = 它不该出现在分母里,只有三类(`*.d.ts` / 测试文件自身 / `*.config.*`);**豁免** = 它**在分母里**、确实没被覆盖、并写明为什么。
🔴 把「不可测」悄悄归进排除项,等于把它伪装成「已覆盖」。豁免理由必须分三类:① 真实未覆盖的防御分支 ② **无注入缝**(想测但产品没留缝,须给出注入失败的实测报错)③ **度量口径产物**。
_Avoid_: 忽略、跳过、白名单

**度量口径产物**(v1.0 Task 1 Phase 2 实测立):
覆盖率数字里**不由代码质量决定、而由度量链路本身造成**的那一部分。本项目的确切成因:lcov 的 `LF` 恒等于**源文件总行数**(空行 / 注释 / 被 esbuild 擦除的类型声明行全进分母),`FN` 里混着 esbuild 注入的 `__name` / `__export` / `__copyProps` / `__toCommonJS`,报出的「未覆盖行号」是**转译后坐标**。⇒「逐文件行覆盖 100%」对含类型声明 / 多导出的文件**不可达**。
⚠️ **判为口径产物必须带反证**(如「该文件函数覆盖 100% ⇒ 那唯一的语句执行过 ⇒ 行号只能是产物」);给不出反证的一律算真实缺口。🚫 不许用它打包解释一切。
_Avoid_: 工具 bug、噪音、误报

**代码面判据**(v1.0 Task 1 Phase 2 立):
「某符号 / 某措辞永不出现」这类判据,**只能扫代码面**(剔掉整行注释与行尾 `//` 注释)。理由:**记录这条红线的注释里必然写着那个符号**,扫全文等于恒命中自己 —— 那是一次恒定的**假红**,而假红会把真红埋掉(本项目已三次同形态)。
⚠️ 剔注释之后必须补一条**判据自检**:同一个扫描器拿到一行真引用时**抓得住**。否则过滤器写成「恒返回空」时判据同样绿。
⚠️ **通用纪律**:任何新判据落盘前必须跑一次反向探针(故意改坏一次)—— 恒绿 / 恒红的假灯与真灯输出一模一样;这条管所有判据,不只零命中 grep(v1.0 Task 6 落,源自 Task 1 / 2 共七条判据缺陷)。
_Avoid_: 零命中 grep(不加限定时会被照抄成扫全文)

**故障注入**(测试手段,v1.0 Task 1 Phase 2 立):
让「备份失败」「删除失败」这类**防御分支**跑起来的唯一办法:`t.mock.method(require('fs'), 'copyFileSync', …)` 抛错。
⚠️ **边界是实测出来的,不是猜的**:只有 **Node 内建模块**能这么注入。产品侧模块经 tsx/esbuild 转 CJS 后导出是 `{"enumerable":true,"configurable":false}` 的**只读 getter**(无 `value`),`mock.method` 报 `The argument 'methodName' must be a method`。⇒ 凡是只能从产品模块内部拿到的对象(如 `initDatabase` 内部 `createDatabase` 出来的 db 句柄),它的失败分支**无法自动化覆盖**,只能进豁免的第 ② 类。
⚠️ `import * as fs` 拿到的是**冻结的命名空间对象**,`mock.method` 会报 `Cannot redefine property` —— 必须用 `createRequire(import.meta.url)('fs')` 取可变的 CJS 导出对象。
_Avoid_: mock、打桩(不加限定时看不出这条边界)
